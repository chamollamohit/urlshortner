<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortener Dashboard</title>
    <link rel="icon" type="image/svg+xml" href="favicon.svg">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 font-sans">

    <div class="container mx-auto p-4 sm:p-6 lg:p-8">

        <header class="flex justify-between items-center mb-8">
            <h1 id="user" class="text-2xl sm:text-3xl font-bold text-gray-800">
                Welcome, <%= user.name %>!
            </h1>
            <button
                class="bg-red-500 hover:bg-red-600 text-white font-bold py-2 px-4 rounded-lg shadow-md transition duration-300 ease-in-out"
                id="logout-btn">
                Logout
            </button>
        </header>

        <main>
            <div class="bg-white p-6 rounded-xl shadow-lg mb-8">
                <form id="url-form">
                    <h2 class="text-xl font-semibold text-gray-700 mb-4">Shorten a new URL</h2>
                    <div class="flex flex-col sm:flex-row gap-4">
                        <input type="url" name="urlinput" placeholder="https://enter-your-long-url-here.com" required
                            class="flex-grow p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 transition" />
                        <button type="submit"
                            class="bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-3 px-6 rounded-lg shadow-md transition duration-300 ease-in-out">
                            Shorten URL
                        </button>
                    </div>
                </form>
            </div>

            <div>
                <h2 class="text-xl font-semibold text-gray-700 mb-4">Your Shortened URLs</h2>
                <div id="url-div" class="space-y-4">
                </div>
            </div>
        </main>
    </div>
    <script src="script.js"></script>
</body>

</html>